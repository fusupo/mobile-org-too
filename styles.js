import { StyleSheet } from 'react-native';

const appStyles = StyleSheet.create({
  baseText: {
    fontFamily: 'Menlo',
    fontSize: 12
  },
  container: {
    flex: 1
  },
  border: {
    borderRadius: 2,
    borderWidth: 0.5,
    borderColor: '#333'
    // padding: 10
  }
});

export default appStyles;
